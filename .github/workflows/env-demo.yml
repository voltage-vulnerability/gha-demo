name: Env Demo

on: workflow_dispatch
env:
 APP_ENV: "at_workflow"

jobs:
  env_demo:
   env:
    APP_ENV: "at_job_level"
   runs-on: ubuntu-slim
   steps:
     - name: Connecting to dev
       run: echo "App is connecting to $APP_ENV"
