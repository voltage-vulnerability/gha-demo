name: variable demo

on: workflow_dispatch

jobs:
  printVariable:
   runs-on: ubuntu-slim
   steps:
     - name: variable value
       run: |
        echo ${{ vars.my_variable}}
        echo "Variable value printed"

       
