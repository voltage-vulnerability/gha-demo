name: Build and Deploy
on:
 pull_request:
  types:
   - opened
   - synchronize

jobs:
   build:
    runs-on: ubuntu-slim
    steps:
     - name: Build step
       run: echo "Building the app......"
   
