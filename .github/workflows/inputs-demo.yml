name: Inputs demo

on:
 workflow_dispatch:
  inputs:
   environment:
     description: Choose environment to deploy
     required: true
     default: dev
     
jobs:
 input-demo:
  runs-on: ubuntu-slim
  steps:
    - name: deploy app
      run: echo "Deploying the application to ${{ inputs.environment }}"
